# .urihandler cache

This directory is reserved for persistent URI translation cache entries.

Runtime-generated `*.json` entries are intentionally ignored by Git. The
portable source of truth remains the URI registry and the v2 translation
strategy.
